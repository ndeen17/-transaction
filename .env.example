# Local dev default — used by `npm run dev`.
# The production build (`npm run build`) instead uses .env.production automatically,
# which points at the deployed Render backend. See Vite's mode-based env file docs.
VITE_API_BASE_URL=http://localhost:4000/api/v1
